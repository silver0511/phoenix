单聊流程：
    1. 在客户端A发送单聊包给用户B以后，服务器收到包以后，先存一条离线信息。
        有两种情况，如果B在线，直接推送ServerRQ给客户端，服务器在收到客户端ServerRS以后，根据message_id删除离线信息。
        如果不在线，直接存离线就ok了。

    2. 在客户端登录的时候需要同步离线信息，每次同步固定个数的离线信息，直到服务器告诉客户端完成，停止发包。